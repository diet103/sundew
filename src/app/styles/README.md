# Sundew styles

Tokens in `tokens.css` are hand-copied from the resume site's Field Notebook system (`resume-website/src/styles/global.css`), including its theme mechanism (light base on `:root`, dark under `:root.theme-dark`).
They must not drift ahead of that source — change the site first, then mirror the values here.
